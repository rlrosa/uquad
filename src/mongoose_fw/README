Code on Mongoose based on code from CKDevices.

Compiles with arduino, after telling arduino about the libraries for handling the barometer.

How to:
	sudo apt-get install arduino
	ln -s <path_to_uquad>/src/mongoose_fw/HMC58X3/ /usr/share/arduino/libraries/
