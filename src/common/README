Description of files in src/common

# uquad_aux_io.[hc]
support for handling multiple io devices. implements polling via select to avoid locking when checking for new data.

# uquad_aux_time.[hc]
implementation of functions tu handle timeval structs.

# uquad_error_codes.h
Defines to use when returning errors.
Macros for error checking.
Macros for logging.

# uquad_types.h
useful types, such as boolean, which is not included in std C.
general parameter definition.

#uquad_config.h
configuration of main.c

#manual_mode.h
macros to be used when controlling main.c from keyboard is implemented.
