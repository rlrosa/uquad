Logs probando los cambios a la IMU.
El objetivo es analizarlos depsues de tener listo de deco binario, para verificar que todo funcione bien (es complicado ver cosas en hexa).

Para abrir archivos binarios se puede usar el emacs, abriendo el archivo como cualquiera y luego haciendo:
     Alt+x RET hexl-mode RET
